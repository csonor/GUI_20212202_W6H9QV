<Window x:Class="ColorCross.GameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorCross"
		xmlns:vm="clr-namespace:ColorCross.ViewModel"
        xmlns:ui="clr-namespace:ColorCross.UI"
        mc:Ignorable="d"
        Title="GameWindow" Height="450" Width="800"
		Closed="Window_Closed">
	<Window.Resources>
        <ui:IntToColorConverter x:Key="IntToColorConverter" />
        <DataTemplate x:Key="DataTemplate_colors">
            <Button Content="{Binding Path=Color}" Background="{Binding Path=Color, Converter={StaticResource IntToColorConverter}}" Click="ColorButton_Click"  Height="20" Width="20" Margin="1,1,1,1"/>
        </DataTemplate>
        <DataTemplate x:Key="DataTemplate_Level2">
			<Button Content="{Binding Path=Color}" Background="{Binding Path=Color, Converter={StaticResource IntToColorConverter}}" Click="Button_Click"  Height="20" Width="20" Margin="1,1,1,1"/>
		</DataTemplate>

		<DataTemplate x:Key="DataTemplate_Level1">
			<ItemsControl ItemsSource="{Binding}" ItemTemplate="{DynamicResource DataTemplate_Level2}">
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<StackPanel Orientation="Horizontal"/>
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>
			</ItemsControl>
		</DataTemplate>

	</Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
		<ItemsControl x:Name="lst" ItemTemplate="{DynamicResource DataTemplate_Level1}" Grid.Column="0"/>
        <ItemsControl x:Name="lst2" ItemTemplate="{DynamicResource DataTemplate_colors}" Grid.Column="1"/>
    </Grid>
</Window>
